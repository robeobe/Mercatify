{
  "name": "list_catalog_capabilities",
  "kind": "pure",
  "backedBy": "src/catalog.ts#getCatalogTool",
  "description": "List the capability keys the catalog knows for one SaaS tool (e.g. HubSpot -> [\"contacts\",\"companies\",\"deals\",\"pipeline\",\"email_sequences\"]). Empty array when the tool is not in the catalog. Use this BEFORE classifying free-text usage notes into capabilities, so you tag with real catalog vocabulary instead of inventing names.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "toolName": { "type": "string", "minLength": 1, "description": "SaaS tool name, e.g. \"HubSpot\"." }
    },
    "required": ["toolName"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "toolName": { "type": "string" },
      "capabilities": { "type": "array", "items": { "type": "string" } },
      "inCatalog": { "type": "boolean" }
    },
    "required": ["toolName", "capabilities", "inCatalog"]
  }
}
